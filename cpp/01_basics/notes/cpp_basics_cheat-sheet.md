# Cheat-Sheet

```c++
// Variablendeklaration und Initialisierung
int x = 5;
double y = 3.14;
char c = 'A';
bool flag = true;

// Kontrollstrukturen
if (x > 0) {
    // Code
} else {
    // Code
}

// Schleifen
for (int i = 0; i < 10; i++) {
    // Code
}

while (x > 0) {
    // Code
    x--;
}

// Funktionen
int add(int a, int b) {
    return a + b;
}

// Arrays und Vektoren
int arr[5] = {1, 2, 3, 4, 5};
std::vector<int> vec = {1, 2, 3, 4, 5};

// Strings
std::string str = "Hello, World!";
```
